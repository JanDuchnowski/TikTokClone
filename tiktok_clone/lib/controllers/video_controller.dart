import 'package:flutter/material.dart';

class VideController extends StatelessWidget {
  const VideController({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
